# 事故卷轴 · 数不清的客人

老板娘想数一数今天来了几位客人。她在门外放了块计数牌，写着 visits = 0。可每次有客人进来，机器就说"这个变量在赋值前就被用了"。

它看不见门外那块牌。在它眼里，函数里那个 visits 是**另一个人**——一个还没出生就被点名的人。

---

给大人的话：作用域是 Python 新手第二大拦路虎。规则很简单：赋值让变量变局部。想要全局，就 global，但更推荐的做法是别碰全局——用参数传进去、用返回值带出来，让咒语各管各的。
