# 事故卷轴 · 一模一样却说不配

老板娘拿了两把钥匙，看起来一模一样，她想让魔法镜判断"配不配对"。镜子上上下下照了半天，说：不配。

她气得不行。可镜子没说错——它只是被问了另一个问题：**你们是不是同一把？** 两把钥匙虽然内容一样，但不是同一件东西。

---

给大人的话：`is` 和 `==` 的差别，是 Python 最著名的"差不多就踩坑"。真实代码里，拿 is 去比较列表内容，会让你半夜调试一个"明明相等却不相等"的幽灵 bug。记住：值用 ==，身份用 is，None 用 is。
